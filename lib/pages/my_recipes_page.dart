import 'package:flutter/material.dart';

class MyRecipesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('My Recipes Page Content')));
  }
}
